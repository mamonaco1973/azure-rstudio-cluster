# Example: install gsl into /efs/rlibs

install.packages("gsl", lib = "/efs/rlibs")

# Example: install gsl into personal library

# install.packages("gsl")

# Example: install ggplot2 into /efs/rlibs

# install.packages("ggplot2", lib = "/efs/rlibs")

# Example: install ggplot2 into personal library

# install.packages("ggplot2")
